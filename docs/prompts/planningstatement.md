Right okay so we've got:
Executive summary, which will have a format but to be honest this can be run last because it's going to be actually summarising the rest of the document so you need to think about that in the future.
- Introduction. This has a section called "About applicant" which will be verbatim imported from somewhere else. I'll explain where later. It will have document scope, which will be a list of my document logs that we've got in the planning application tool.
- Next section is site surroundings and planning history. Again these will import site surroundings. Actually don't worry about that yet. We'll just explain the sections.
- Proposed development section, which proposes and describes what we're building.
- Background section, where we'll have some imports from some things like pre-app EIA, statement of community involvement, etc.
- Policy section, where we will basically describe the policies in a particular way.
- Planning assessment, which we've already done.
- Conclusion for now. Again conclusion will have the format but that will be run after because it will just conclude what we've already done.
Does that make sense for the sections so far? Maybe it's best to set them out and then we can start working on the prompts and templates for each one. What do you think?